foo <search jobs *> => job-routes(),
    <search jobs *> => job-routes(),
    <search jobs *> => job-routes(),
    <search jobs *> => job-routes(),
    <search jobs *> => job-routes(),
    <search jobs *> => job-routes();