my @a=1 ,2 ,3 ,n=>42;

my $bind:='bind';

1+( 2*3 )/4;

@a.grep( * ~~Int ).grep( *.is-prime );

for @a ->$item {
    $item.Str~~/ ( a && b )||[ c d * e**42 ] /
}

say $bind.comb( 2 );

my $array=[ 1 ,! True ];
my $hash={ :a ,:b };
